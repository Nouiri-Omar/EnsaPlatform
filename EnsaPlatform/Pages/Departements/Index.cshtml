@page
@model EnsaPlatform.Pages.Departements.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Departement[0].TITRE)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Departement) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TITRE)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DepartementID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DepartementID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DepartementID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
