@page
@model EnsaPlatform.Pages.Matieres.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Matiere[0].ProfesseurID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Matiere[0].TITRE)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Matiere) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Professeur.NOM)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TITRE)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.MatiereID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.MatiereID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.MatiereID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
