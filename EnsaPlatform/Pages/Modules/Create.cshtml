@page
@model EnsaPlatform.Pages.Modules.CreateModel

@{
    ViewData["Title"] = "Create";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Create</h1>

<h4>Module</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Module.ModuleID" class="control-label"></label>
                <input asp-for="Module.ModuleID" class="form-control" />
                <span asp-validation-for="Module.ModuleID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Module.ProfesseurID" class="control-label"></label>
                <select asp-for="Module.ProfesseurID" class="form-control" asp-items="ViewBag.Prof"></select>
            </div>
            <div class="form-group">
                <label asp-for="Module.TITRE" class="control-label"></label>
                <input asp-for="Module.TITRE" class="form-control" />
                <span asp-validation-for="Module.TITRE" class="text-danger"></span>
            </div>
            <div class="form-group">
                <div class="table">
                    <table>
                        <tr>
                            @{
                                int cnt = 0;

                                foreach (var matiere in ViewBag.matiere)
                                {
                                    if (cnt++ % 3 == 0)
                                    {
                                    @:</tr><tr>
                                    }
                                    @:<td>
                                        <input type="checkbox"
                                               name="selectedMatiere"
                                               value="@matiere.MatiereID"
                                               @(Html.Raw(matiere.Assigned ? "checked=\"checked\"" : "")) />
                                        @matiere.MatiereID @:  @matiere.TITRE
                                    @:</td>
                                }
                            @:</tr>
                            }
                        </table>
                    </div>
                </div>
                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                </div>
            </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
